stages:
  - test
  - package

test backend:
  stage: test
  script:
    - echo "this is test stage"

package_backend:
  stage: package
  image: docker:19.03.12
  services:
    - docker:19.03.12-dind
  before_script:
    - echo "$CI_GITLAB_TOKEN" | docker login -u "$CI_REGISTRY_USER" --password-stdin "$CI_REGISTRY"
  script:
    - docker build -t $CI_REGISTRY_IMAGE:$COMMIT_REF_NAME-$CI_PIPELINE_ID .
    - docker push $CI_REGISTRY_IMAGE:$COMMIT_REF_NAME-$CI_PIPELINE_ID
  




